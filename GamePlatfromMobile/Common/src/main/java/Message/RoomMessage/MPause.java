package Message.RoomMessage;

import Message.Common.MessageRequest;

public class MPause extends MessageRequest {
    public MPause(String account, String validateCode) {
        super(account, validateCode);
    }
}
